import React from 'react';
import './Model.scss';

const Model = () => {
    return (
        <div className='model'>
            <span>Model section is unavailable</span>
        </div>
    );
}

export default Model;